language: node_js
node_js:
  - "6"
  - "7"
before_install:
  - sudo apt-get update && sudo apt-get install oracle-java8-installer
script:
  - sudo npm install -g instagram-profilecrawl
  - instagram-profilecrawl nacim_goura --chrome